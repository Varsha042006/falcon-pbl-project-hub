import Link from "next/link"; import { listProjects } from "@/lib/data";
export default async function Projects(){const ps=await listProjects();return <section className="section"><div className="container"><h1>Explore Project Ideas</h1><p className="lead">Browse faculty-published projects by domain, technology and allocation status.</p><div className="grid">{ps.map(p=><article className="card" key={p.id}><span className="status">{p.status}</span><h2>{p.title}</h2><p>{p.short_description}</p><p><b>Domain:</b> {p.domain}<br/><b>Suitable for:</b> {p.suitable_semester}<br/><b>Publisher:</b> {p.publisher_name}</p><p>{p.allocated_count} of {p.max_teams} team slots allocated</p><Link className="btn" href={`/projects/${p.id}`}>View project</Link></article>)}</div></div></section>}
