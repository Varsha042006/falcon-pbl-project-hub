import { requireRole } from "@/lib/auth";import { redirect } from "next/navigation";
export default async function Imports(){if(!await requireRole(['ADMIN','COORDINATOR']))redirect('/login');return <section className="section"><div className="container"><h1>Master Data Imports</h1><div className="grid"><div className="card"><h2>Faculty Master</h2><p>Upload and validate faculty code, name, department, designation, email, phone and active status.</p><p className="notice">V1 ships with CSV templates and documented SQL/import service extension points. Production bulk import UI is marked as the next implementation unit.</p><a className="btn secondary" href="/templates/faculty_master_template.csv">Download template</a></div><div className="card"><h2>Student Master</h2><p>Upload USN, name, semester, section, contact information, mentor and supervisor mapping.</p><a className="btn secondary" href="/templates/student_master_template.csv">Download template</a></div></div></div></section>}
